import { Button, Typography } from "@mui/material";
import { Layout } from "../components/layout";

const Home = () => {
  return (
    <Layout>
      <Typography variant="h4">SpotiFree</Typography>
    </Layout>
  );
};

export default Home;
